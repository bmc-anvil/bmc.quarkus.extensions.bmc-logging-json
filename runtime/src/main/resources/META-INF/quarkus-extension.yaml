---
name: "BMC logging json"
artifact: ${project.groupId}:${project.artifactId}:${project.version}
description: "Logging structured JSON formatting support"
metadata:
    keywords:
        - "core"
        - "formatter"
        - "jackson"
        - "json"
        - "logging"
        - "structured"
    categories:
        - "core"
        - "logging"
    status: "experimental"
    guide: ""
    config:
        - "quarkus.log.console.json."
        - "quarkus.log.file.json."
    built-with-quarkus-core: "3.18.3"
    requires-quarkus-core: "[3.18,)"
    minimum-java-version: "17"
    extension-dependencies:
        - "io.quarkus:quarkus-arc"
        - "io.quarkus:quarkus-core"
        - "io.quarkus:quarkus-jackson"
